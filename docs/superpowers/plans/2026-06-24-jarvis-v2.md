# Jarvis v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bürküt sesli asistanını doğal dil anlayan Jarvis'e dönüştür; Groq NLU ile hava/hatırlatıcı/genel soru desteği ekle ve Telegram'dan PC ekranına overlay bildirim gönder.

**Architecture:** Sesli komutlar STT'den sonra Groq'a intent analizi için gönderilir, dönen JSON'a göre mevcut handler'lara yönlendirilir. `/bildirim` Telegram komutu `pc_agent` üzerinden yeni `core/notifier.py`'deki tkinter overlay'i tetikler. Hatırlatıcı zamanı gelince hem TTS hem Telegram hem overlay çalışır.

**Tech Stack:** Python 3.11, groq (llama-3.3-70b-versatile), tkinter (built-in), python-telegram-bot, Edge-TTS

## Global Constraints

- Python 3.11, `C:\Users\Kayra\Pcbot` proje kökü
- Groq client: `groq.Groq(api_key=os.environ.get("GROQ_API_KEY"))`, model `llama-3.3-70b-versatile`
- TTS: `text_to_speech.speak(str)` — `voice/text_to_speech.py`
- PC agent action kaydı: `_HANDLERS` dict — `local/pc_agent.py:343`
- Telegram handler kaydı: `main.py` `setup_handlers()` fonksiyonu
- `@authorized_only` dekoratörü tüm yeni Telegram handler'larında kullanılmalı

---

## File Map

| Dosya | Durum | Sorumluluk |
|---|---|---|
| `core/notifier.py` | Yeni | tkinter overlay pencere |
| `local/pc_agent.py` | Değişecek | `_cmd_show_notification` + `_HANDLERS` kaydı |
| `bot/handlers.py` | Değişecek | `cmd_bildirim()` |
| `main.py` | Değişecek | `/bildirim` CommandHandler kaydı |
| `voice/voice_assistant.py` | Değişecek | `_groq_nlu()` + `_handle_intent()` + `_execute()` refactor |
| `ai/calendar_mgr.py` | Değişecek | `set_tts_callback()` + `_check_loop()` TTS çağrısı |

---

## Task 1: Ekran Overlay Penceresi

**Files:**
- Create: `core/notifier.py`

**Interfaces:**
- Produces: `show_overlay(text: str, duration: int = 8) -> None`

- [ ] **Adım 1: `core/notifier.py` dosyasını oluştur**

```python
import threading
import tkinter as tk


def show_overlay(text: str, duration: int = 8) -> None:
    """Ekran ortasında duration saniye görünen bildirim popup'ı."""
    def _run():
        root = tk.Tk()
        root.overrideredirect(True)
        root.attributes("-topmost", True)
        root.attributes("-alpha", 0.92)
        root.configure(bg="#1a1a2e")

        sw = root.winfo_screenwidth()
        sh = root.winfo_screenheight()

        frame = tk.Frame(root, bg="#1a1a2e", padx=30, pady=20)
        frame.pack()

        tk.Label(
            frame,
            text="🦅 BÜRKÜT",
            font=("Segoe UI", 11, "bold"),
            fg="#e94560",
            bg="#1a1a2e",
        ).pack()

        tk.Label(
            frame,
            text=text,
            font=("Segoe UI", 16),
            fg="#ffffff",
            bg="#1a1a2e",
            wraplength=500,
            justify="center",
        ).pack(pady=(8, 0))

        root.update_idletasks()
        w = root.winfo_reqwidth()
        h = root.winfo_reqheight()
        root.geometry(f"{w}x{h}+{(sw - w) // 2}+{(sh - h) // 2}")

        root.bind("<Button-1>", lambda e: root.destroy())
        root.after(duration * 1000, root.destroy)
        root.mainloop()

    threading.Thread(target=_run, daemon=True).start()
```

- [ ] **Adım 2: Manuel test**

```python
# Python REPL'de çalıştır
import sys; sys.path.insert(0, r"C:\Users\Kayra\Pcbot")
from core.notifier import show_overlay
show_overlay("Merhaba efendim! Bu bir test bildirimidir.", duration=5)
import time; time.sleep(6)
```

Beklenen: Ekran ortasında koyu arka planlı, kırmızı "🦅 BÜRKÜT" başlıklı, beyaz metinli popup 5 saniye görünür sonra kapanır.

- [ ] **Adım 3: Commit**

```bash
git add core/notifier.py
git commit -m "feat: tkinter overlay bildirim penceresi (core/notifier.py)"
```

---

## Task 2: Telegram → PC Overlay Pipeline (`/bildirim`)

**Files:**
- Modify: `local/pc_agent.py` — `_cmd_show_notification` fonksiyonu + `_HANDLERS` kaydı
- Modify: `bot/handlers.py` — `cmd_bildirim()` async handler
- Modify: `main.py` — `/bildirim` CommandHandler

**Interfaces:**
- Consumes: `show_overlay(text: str, duration: int = 8)` — `core/notifier.py` (Task 1)
- Consumes: `send_to_agent(action, params, timeout)` — `bot/agent_relay.py`
- Produces: `/bildirim` Telegram komutu

- [ ] **Adım 1: `pc_agent.py`'e `_cmd_show_notification` ekle**

`local/pc_agent.py`'de `_cmd_run_claude` fonksiyonunun hemen altına (yaklaşık satır 342):

```python
def _cmd_show_notification(params: dict) -> tuple[bool, object]:
    from core.notifier import show_overlay
    text = params.get("text", "").strip()
    duration = int(params.get("duration", 8))
    if not text:
        return False, "Mesaj boş."
    show_overlay(text, duration=duration)
    return True, "Bildirim gösterildi."
```

- [ ] **Adım 2: `_HANDLERS`'a kaydet**

`local/pc_agent.py`'deki `_HANDLERS` dict'ine (satır ~382, `"press_key"` satırından sonra):

```python
    "show_notification": _cmd_show_notification,
```

- [ ] **Adım 3: `bot/handlers.py`'e `cmd_bildirim` ekle**

`cmd_yap` fonksiyonunun hemen altına (yaklaşık satır 1090):

```python
@authorized_only
async def cmd_bildirim(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """/bildirim [mesaj] — PC ekranında overlay bildirim göster."""
    from bot.agent_relay import send_to_agent
    if not context.args:
        await update.message.reply_text(
            "❌ Kullanım: `/bildirim [mesaj]`\nÖrnek: `/bildirim Toplantı 5 dk sonra!`",
            parse_mode="Markdown",
        )
        return
    text = " ".join(context.args)
    result = await send_to_agent("show_notification", {"text": text}, timeout=10.0)
    if result and result.get("ok"):
        await update.message.reply_text("✅ Bildirim gönderildi.")
    else:
        err = result.get("data", "PC agent çevrimiçi değil.") if result else "PC agent çevrimiçi değil."
        await update.message.reply_text(f"❌ {err}")
```

- [ ] **Adım 4: `main.py`'e kaydet**

`main.py`'de `/tus` handler satırından sonra:

```python
    app.add_handler(CommandHandler("bildirim", handlers.cmd_bildirim))
```

- [ ] **Adım 5: Test**

1. `local/pc_agent.py`'i çalıştır (veya mevcut process varsa yeniden başlat)
2. Telegram'dan `/bildirim Merhaba dünya!` gönder
3. PC ekranında overlay çıkmalı, Telegram'dan "✅ Bildirim gönderildi." dönmeli

- [ ] **Adım 6: Commit**

```bash
git add local/pc_agent.py bot/handlers.py main.py
git commit -m "feat: /bildirim komutu — Telegram'dan PC overlay bildirimi"
```

---

## Task 3: Groq NLU Voice Pipeline

**Files:**
- Modify: `voice/voice_assistant.py` — `_groq_nlu()` + `_handle_intent()` + `_execute()` refactor

**Interfaces:**
- Consumes: `groq.Groq(api_key=...)` + `llama-3.3-70b-versatile`
- Consumes: `news_weather.get_weather(city: str) -> str`
- Consumes: `calendar_mgr.add_calendar_event(chat_id: int, text: str, when_str: str) -> tuple[bool, str]`
- Consumes: `calendar_mgr.get_reminders_text(chat_id: int) -> str`
- Consumes: `text_to_speech.speak(str)` (mevcut)
- Consumes: `parse(text) -> tuple[str, list]` — `voice/command_parser.py` (sistem komutları için tutulur)

- [ ] **Adım 1: `voice_assistant.py` dosyasını aç, import bloğuna ekle**

Dosyanın en üstündeki import'ların sonuna:

```python
import os
import json
import groq as _groq
from dotenv import load_dotenv
load_dotenv()

_groq_client = _groq.Groq(api_key=os.environ.get("GROQ_API_KEY"))

_NLU_SYSTEM = """Sen Bürküt'sün. Kullanıcının Türkçe sesli komutunu analiz et ve SADECE JSON döndür.

{"intent": "...", "params": {...}}

Intent seçenekleri:
- "weather": {"city": "şehir adı, belirtilmediyse Istanbul"}
- "reminder_set": {"text": "hatırlatma içeriği", "when": "zaman ifadesi (örn: yarın sabah, 5 dakika sonra, 15:30)"}
- "reminder_list": {}
- "general": {"answer": "Türkçe kısa ve doğal cevap, maksimum 2 cümle"}
- "system": {"cmd": "shutdown|restart|sleep|hibernate|screenshot|status|volume_up|volume_down|volume_set|mute|lock|clipboard|gpu|brightness_set|windows|help|launch|open_file|open_url|open_folder|close_proc", "args": []}

Sadece JSON döndür. Başka hiçbir şey yazma."""
```

- [ ] **Adım 2: `_groq_nlu()` fonksiyonu ekle**

`_execute` fonksiyonunun hemen üstüne:

```python
def _groq_nlu(text: str) -> dict:
    """STT metnini Groq'a gönderir, intent JSON döndürür. Hata durumunda general döner."""
    try:
        completion = _groq_client.chat.completions.create(
            model="llama-3.3-70b-versatile",
            messages=[
                {"role": "system", "content": _NLU_SYSTEM},
                {"role": "user", "content": text},
            ],
            max_tokens=256,
            temperature=0.1,
        )
        raw = completion.choices[0].message.content.strip()
        # Kod bloğu varsa temizle
        if raw.startswith("```"):
            raw = raw.split("```")[1]
            if raw.startswith("json"):
                raw = raw[4:]
        return json.loads(raw)
    except Exception as e:
        print(f"[NLU] Groq hatası: {e}")
        return {"intent": "general", "params": {"answer": "Anlayamadım efendim."}}
```

- [ ] **Adım 3: `_handle_intent()` fonksiyonu ekle**

`_groq_nlu` fonksiyonunun hemen altına:

```python
def _handle_intent(intent: str, params: dict) -> None:
    """NLU sonucunu işler ve TTS ile yanıt verir."""
    try:
        if intent == "weather":
            from ai.news_weather import get_weather
            city = params.get("city", "Istanbul")
            full = get_weather(city)
            # TTS için kısa özet al
            lines = [l for l in full.split("\n") if "°C" in l or "Durum" in l]
            summary = _groq_client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[
                    {"role": "system", "content": "Aşağıdaki hava durumu verisini sesli asistan için 1-2 cümleye özetle. Sadece metin yaz, emoji yok."},
                    {"role": "user", "content": full},
                ],
                max_tokens=80,
            ).choices[0].message.content.strip()
            text_to_speech.speak(summary)

        elif intent == "reminder_set":
            from ai.calendar_mgr import add_calendar_event
            reminder_text = params.get("text", "")
            when_str = params.get("when", "")
            ok, msg = add_calendar_event(chat_id=0, text=reminder_text, when_str=when_str)
            if ok:
                text_to_speech.speak(f"Tamam efendim, {when_str} için hatırlatırım.")
            else:
                text_to_speech.speak("Zamanı anlayamadım, tekrar söyler misiniz?")

        elif intent == "reminder_list":
            from ai.calendar_mgr import get_reminders_text
            reminders = get_reminders_text(chat_id=0)
            if "yok" in reminders.lower():
                text_to_speech.speak("Aktif hatırlatmanız yok efendim.")
            else:
                # Hatırlatmaları sesli oku (markdown işaretlerini temizle)
                import re
                clean = re.sub(r"[*_`#━]", "", reminders)
                text_to_speech.speak(clean[:400])

        elif intent == "general":
            answer = params.get("answer", "Anlayamadım.")
            text_to_speech.speak(answer)

        elif intent == "system":
            cmd = params.get("cmd", "unknown")
            args = params.get("args", [])
            _execute_system(cmd, args)

        else:
            text_to_speech.speak("Anlayamadım efendim.")

    except Exception as e:
        print(f"[NLU] Intent işleme hatası ({intent}): {e}")
        text_to_speech.speak("Bir hata oluştu efendim.")
```

- [ ] **Adım 4: `_execute_system()` fonksiyonu ekle — mevcut sistem komutlarını taşı**

`_handle_intent` fonksiyonunun hemen altına. Mevcut `_execute()` içindeki tüm `if command == ...` blokları buraya taşınır, `command` → `cmd` ve `args` parametresi eklenir:

```python
def _execute_system(cmd: str, args: list) -> None:
    """Sistem komutlarını çalıştırır (shutdown, screenshot, ses vs.)"""
    try:
        if cmd == "shutdown":
            text_to_speech.speak("Bilgisayar kapatılıyor.")
            from core.power_manager import immediate_shutdown
            immediate_shutdown()

        elif cmd == "restart":
            text_to_speech.speak("Yeniden başlatılıyor.")
            from core.power_manager import immediate_restart
            immediate_restart()

        elif cmd == "sleep":
            text_to_speech.speak("Uyku moduna geçiliyor.")
            from core.power_manager import sleep_pc
            sleep_pc()

        elif cmd == "hibernate":
            text_to_speech.speak("Hazırda bekleme moduna geçiliyor.")
            from core.power_manager import hibernate_pc
            hibernate_pc()

        elif cmd == "screenshot":
            import mss
            from PIL import Image
            import datetime
            with mss.mss() as sct:
                monitor = sct.monitors[1]
                screenshot = sct.grab(monitor)
                img = Image.frombytes("RGB", screenshot.size, screenshot.bgra, "raw", "BGRX")
                path = Path.home() / "Desktop" / f"ekran_{datetime.datetime.now().strftime('%H%M%S')}.jpg"
                img.save(str(path), "JPEG", quality=85)
            text_to_speech.speak("Ekran görüntüsü masaüstüne kaydedildi.")

        elif cmd == "status":
            from core.system_info import get_cpu_info, get_ram_info, get_gpu_info
            cpu = get_cpu_info()
            ram = get_ram_info()
            gpu = get_gpu_info()
            msg = f"CPU yüzde {cpu['percent']:.0f}, RAM yüzde {ram['percent']:.0f} kullanımda."
            if gpu["available"] and gpu["gpus"]:
                g = gpu["gpus"][0]
                msg += f" GPU yüzde {g['usage']} kullanımda, sıcaklık {g['temp']} derece."
            text_to_speech.speak(msg)

        elif cmd == "volume_up":
            from core.audio_controller import volume_up
            new = volume_up(10)
            text_to_speech.speak(f"Ses yüzde {new}.")

        elif cmd == "volume_down":
            from core.audio_controller import volume_down
            new = volume_down(10)
            text_to_speech.speak(f"Ses yüzde {new}.")

        elif cmd == "volume_set":
            level = args[0] if args else None
            if level is not None:
                from core.audio_controller import set_volume
                new = set_volume(int(level))
                text_to_speech.speak(f"Ses yüzde {new} olarak ayarlandı.")

        elif cmd == "mute":
            from core.audio_controller import toggle_mute
            muted = toggle_mute()
            text_to_speech.speak("Sessiz." if muted else "Ses açık.")

        elif cmd == "lock":
            text_to_speech.speak("Ekran kilitleniyor.")
            from core.display_manager import lock_screen
            lock_screen()

        elif cmd == "clipboard":
            from core.clipboard_manager import get_clipboard
            content = get_clipboard()
            if content:
                text_to_speech.speak(f"Panoda: {content[:80]}")
            else:
                text_to_speech.speak("Pano boş.")

        elif cmd == "gpu":
            from core.system_info import get_gpu_info
            gpu = get_gpu_info()
            if gpu["available"] and gpu["gpus"]:
                g = gpu["gpus"][0]
                text_to_speech.speak(f"{g['name']}: yüzde {g['usage']} kullanım, {g['temp']} derece.")
            else:
                text_to_speech.speak("GPU bilgisi alınamadı.")

        elif cmd == "brightness_set":
            level = args[0] if args else None
            if level is not None:
                from core.display_manager import set_brightness
                set_brightness(int(level))
                text_to_speech.speak(f"Parlaklık yüzde {level} olarak ayarlandı.")

        elif cmd == "windows":
            from core.window_manager import get_windows
            windows = get_windows()
            text_to_speech.speak(f"{len(windows)} açık pencere var.")

        elif cmd == "help":
            text_to_speech.speak(
                "Sesli komutlar: hava durumu, hatırlatıcı kur, sistem durumu, "
                "ekran görüntüsü, ses aç, ses kıs, sessiz, kilitle, pano, "
                "bilgisayarı kapat, yeniden başlat, uyku, uygulama adı aç."
            )

        elif cmd == "launch":
            target = args[0] if args else ""
            if target:
                from core.launcher import open_app, open_url
                if any(target.startswith(p) for p in ("http", "www.")):
                    ok, _ = open_url(target)
                else:
                    ok, _ = open_app(target)
                text_to_speech.speak(f"{target} açıldı." if ok else f"{target} açılamadı.")
            else:
                text_to_speech.speak("Ne açmak istediğinizi söyleyin.")

        elif cmd == "open_file":
            target = args[0] if args else ""
            if target:
                from core.launcher import find_and_open
                ok, _ = find_and_open(target)
                text_to_speech.speak(f"{target} açıldı." if ok else f"{target} bulunamadı.")

        elif cmd == "open_url":
            url = args[0] if args else ""
            if url:
                from core.launcher import open_url
                ok, _ = open_url(url)
                text_to_speech.speak("Tarayıcıda açıldı." if ok else "Açılamadı.")

        elif cmd == "open_folder":
            folder = args[0] if args else ""
            if folder:
                from core.launcher import open_folder
                ok, _ = open_folder(folder)
                text_to_speech.speak("Klasör açıldı." if ok else "Klasör bulunamadı.")

        elif cmd == "close_proc":
            target = args[0] if args else ""
            if target:
                from core.process_manager import kill_process
                ok, _ = kill_process(target)
                text_to_speech.speak(f"{target} kapatıldı." if ok else f"{target} bulunamadı.")

        else:
            text_to_speech.speak("Bu komutu anlayamadım.")

    except Exception as e:
        print(f"[SES] Sistem komutu hatası ({cmd}): {e}")
        text_to_speech.speak("Bir hata oluştu.")
```

- [ ] **Adım 5: `_execute()` fonksiyonunu Groq NLU'ya yönlendir**

Mevcut `_execute` fonksiyonunun tüm içeriğini şununla değiştir:

```python
def _execute(text: str):
    print(f"[SES] NLU analiz ediliyor: '{text}'")
    result = _groq_nlu(text)
    intent = result.get("intent", "general")
    params = result.get("params", {})
    print(f"[SES] Intent: {intent}, params: {params}")
    _notify("transcript", text)
    _handle_intent(intent, params)
```

- [ ] **Adım 6: Test**

1. `local/pc_agent.py`'i başlat (sesli asistan `voice_assistant.py` üzerinden çalışıyor)
2. Alt+Space → "İstanbul'da hava nasıl?" → Groq TTS özet söylemeli
3. Alt+Space → "Yarın sabah toplantı var hatırlat" → "Tamam efendim, yarın sabah için hatırlatırım."
4. Alt+Space → "Python nedir?" → Kısa Türkçe cevap
5. Alt+Space → "Ekran görüntüsü al" → screenshot + TTS

- [ ] **Adım 7: Commit**

```bash
git add voice/voice_assistant.py
git commit -m "feat: Groq NLU voice pipeline — doğal dil hava/hatırlatıcı/genel soru desteği"
```

---

## Task 4: Sesli Hatırlatıcı

**Files:**
- Modify: `ai/calendar_mgr.py` — `set_tts_callback()` + `_check_loop()` TTS + overlay çağrısı
- Modify: `local/pc_agent.py` — başlangıçta TTS callback bağlantısı

**Interfaces:**
- Consumes: `text_to_speech.speak(str)` — `voice/text_to_speech.py`
- Consumes: `show_overlay(text: str, duration: int)` — `core/notifier.py` (Task 1)

- [ ] **Adım 1: `ai/calendar_mgr.py`'e TTS callback ekle**

Dosyanın en üstündeki global değişkenlere (satır ~14, `_notify_callback` satırından sonra):

```python
_tts_callback: Optional[Callable] = None


def set_tts_callback(callback: Callable) -> None:
    """Sesli uyarı için TTS callback — voice_assistant tarafından atanır."""
    global _tts_callback
    _tts_callback = callback
```

- [ ] **Adım 2: `_check_loop()` içinde TTS + overlay çağrısı ekle**

`_check_loop()` içindeki `sent = True` satırından (Telegram gönderim bloğunun hemen sonrasına) ekle:

```python
                if sent:
                    # Sesli uyarı
                    if _tts_callback:
                        try:
                            _tts_callback(f"Efendim, hatırlatmanız var: {rem['text']}")
                        except Exception as e:
                            logger.error(f"TTS hatırlatma hatası: {e}")
                    # Overlay bildirim
                    try:
                        from core.notifier import show_overlay
                        show_overlay(f"⏰ Hatırlatma!\n{rem['text']}", duration=10)
                    except Exception as e:
                        logger.error(f"Overlay hatırlatma hatası: {e}")
                    mark_reminder_done(rem["id"])
```

> **Dikkat:** Mevcut `if sent: mark_reminder_done(rem["id"])` satırını bu yeni blokla tamamen değiştir.

- [ ] **Adım 3: `local/pc_agent.py`'de başlangıçta TTS callback bağla**

`pc_agent.py`'de `_startup_sequence()` fonksiyonunun başına (yaklaşık satır 414):

```python
    # Hatırlatıcı sesli uyarısını TTS'e bağla
    try:
        from voice.text_to_speech import speak as _tts_speak
        from ai.calendar_mgr import set_tts_callback
        set_tts_callback(_tts_speak)
    except Exception:
        pass
```

- [ ] **Adım 4: Test**

```python
# Test — bir dakika sonraya hatırlatıcı kur
from ai.calendar_mgr import add_calendar_event, start_reminder_checker
from ai import calendar_mgr
from voice import text_to_speech

text_to_speech.start()
calendar_mgr.set_tts_callback(text_to_speech.speak)
add_calendar_event(0, "test hatırlatması", "1 dakika sonra")
import asyncio
asyncio.run(start_reminder_checker())  # ya da pc_agent çalışırken Telegram'dan /hatirlatici ekle
```

Beklenen: 1 dk sonra TTS "Efendim, hatırlatmanız var: test hatırlatması" söyler + overlay çıkar.

- [ ] **Adım 5: Commit**

```bash
git add ai/calendar_mgr.py local/pc_agent.py
git commit -m "feat: sesli hatırlatıcı — zamanı gelince TTS + overlay bildirim"
```
